{
  "name": "amnesty-api",
  "version": "1.0.0",
  "description": "Api for the Amnesty International Blog",
  "main": "index.js",
  "author": "Martin Fracker <martin.frackerjr@gmail.com>",
  "license": "GNU GPL v3",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./yarn.lock --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "cors": "^2.8.4",
    "express": "^4.15.4",
    "mongodb": "^2.2.31",
    "rimraf": "^2.6.1"
  }
}
